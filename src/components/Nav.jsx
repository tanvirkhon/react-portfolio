import React from "react";

const Nav = () => {
  return (
    <nav className="mt-4 text-lg">
      <section>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
      </section>
    </nav>
  );
};

export default Nav;
